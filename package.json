{
  "name": "feishu-dsh-bridge",
  "version": "0.1.0",
  "description": "Bidirectional bridge: chat with your DeepSeek Harness agent from Feishu (Lark) — long-connection bot, per-user sessions, approval/question forwarding",
  "keywords": [
    "deepseek-harness",
    "dsh",
    "dsh-plugin",
    "feishu",
    "lark",
    "bot",
    "agent"
  ],
  "license": "MIT",
  "author": "minyang",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/minyang2020/dsh-feishu-bridge.git"
  },
  "type": "module",
  "main": "src/plugin.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/main.js",
    "test:unit": "node --test",
    "test:syntax": "node --check src/main.js && node --check src/plugin.js && node --check src/bridge.js && node --check src/config.js && node --check src/dsh-client.js && node --check src/session-map.js && node --check src/util.js",
    "test": "npm run test:unit && npm run test:syntax",
    "smoke:dsh": "node scripts/smoke-dsh.mjs",
    "test:feishu-ws": "node scripts/test-feishu-ws.mjs",
    "test:roundtrip": "node scripts/test-roundtrip.mjs"
  },
  "dependencies": {
    "@deepseek-ai/schemastery": "^3.18.1",
    "@larksuiteoapi/node-sdk": "^1.73.0"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "files": [
    "src",
    "scripts",
    "cordis.patch.yml",
    "README.md",
    "README.en.md",
    ".env.example"
  ]
}
